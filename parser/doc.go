/*
Package parser reads input and produces commands ( or errors. )
For typical use:
	create a Parser,
	add comprehensions to the parser,
	add patterns to the comprehensions,
	use the parser to match user input to commands.
*/
package parser
