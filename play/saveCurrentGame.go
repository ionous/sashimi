package play

import "github.com/ionous/mars/rt"

func SaveCurrentGame(rt.Runtime, bool) (string, bool) {
	panic("in new york")
	// i := g.(*internal.GameEventAdapter)
	// return i.Game.SaveGame(autosave)
}
